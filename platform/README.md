# Platform Folders

Platform-specific files are grouped here:

- `platform/windows/`
- `platform/linux/`
- `platform/macos/`

Current production CI release path is Windows:
- `.github/workflows/cross-platform-build.yml`

macOS and Linux content is preserved for future native build work.
