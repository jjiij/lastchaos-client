#ifndef SE_INCL_ENGINE_CLASSES_BASEEVENTS_H
#define SE_INCL_ENGINE_CLASSES_BASEEVENTS_H

// Temporary porting stub.
// The original build appears to generate this header from BaseEvents.es.

enum EBaseEventType {
  BET_NONE = 0
};

enum DamageType {
  DMT_EXPLOSION = 1,
  DMT_PROJECTILE = 2,
  DMT_CLOSERANGE = 3,
  DMT_BULLET = 4,
  DMT_DROWNING = 5,
  DMT_IMPACT = 6,
  DMT_BRUSH = 7,
  DMT_BURNING = 8,
  DMT_ACID = 9,
  DMT_TELEPORT = 10,
  DMT_FREEZING = 11,
  DMT_SPIKESTAB = 14,
  DMT_ABYSS = 15,
  DMT_HEAT = 16,
  DMT_DAMAGER = 17,
  DMT_CHAINSAW = 18,
  DMT_BOMB = 19,
  DMT_SHOTGUN = 20,
  DMT_SNIPER = 21,
  DMT_NONE = 9999
};

#endif
