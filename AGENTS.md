# AGENTS

Project execution baseline:

- Keep client source edits under `client-source/`.
- Keep backup trees read-only.
- Keep generated build outputs and unpacked assets out of git.
- Keep platform-specific helper files under `platform/windows`, `platform/linux`, and `platform/macos`.
